<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.dao.SignDao">

	<select id="showAllByEle" resultType="String">
		select sdate from sign
		where qid = #{qid}
		and openid = #{openid}
		and year(sdate) = year(#{sdate})
		and month(sdate) = month(#{sdate})
	</select>
	
	<insert id="addSign" useGeneratedKeys="true" keyProperty="id">
		insert into sign (qid, openid, sdate) value (#{qid}, #{openid}, #{sdate})
	</insert>
</mapper>
